--- a/Lib/libui/__init__.py
+++ b/Lib/libui/__init__.py
@@ -1,3 +1,7 @@
+__package__ = 'libui'
+
+__path__ = [__name__]
+
 """Python bindings for libui-ng — thread-safe proxy wrappers."""
 
 from libui import core
@@ -597,6 +601,11 @@
         )
 
     def insert_at(self, *args, **kwargs):
+        args = list(args)
+        if args and hasattr(args[0], "_core"):
+            args[0] = args[0]._core
+        if len(args) > 1 and hasattr(args[1], "_core"):
+            args[1] = args[1]._core
         core.queue_main(lambda: self._core.insert_at(*args, **kwargs))
 
